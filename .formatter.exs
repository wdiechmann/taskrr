# Used by "mix format"
[
  # inputs list the files mix format should modify
  inputs: ["mix.exs", "config/*.exs"],
  # subdirectories indicate where to find other formatter files.
  subdirectories: ["apps/*"]
]
